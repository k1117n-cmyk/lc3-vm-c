# Lesson 01: Skeleton

This is the fixed code for the first article.

It loads an LC-3 `.obj` file, fetches one instruction at a time, decodes the
opcode, and handles only `TRAP HALT`.

```sh
make
./lc3 programs/halt.obj
```

