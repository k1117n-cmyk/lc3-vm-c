# Lesson 02: Instruction Set

This is the fixed code for the second article.

It adds the main LC-3 instructions, `sign_extend()`, and `update_flags()`.
Terminal I/O and the full `TRAP` implementation are intentionally left for the
third article.

```sh
make
./lc3 image.obj
```

